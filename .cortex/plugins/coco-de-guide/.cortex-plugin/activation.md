This plugin supports the Data Engineering with CoCo quickstart guide.

It includes:
- **Skills** — the `new-dbt-model` skill for creating dbt models that follow project conventions
- **Agents** — subagents for delegating specialized tasks
- **Hooks** — shell commands that run automatically in response to tool events
